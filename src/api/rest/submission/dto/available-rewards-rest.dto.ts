import { AvailableRewardDto, AvailableRewardsResponseDto } from '../../../../modules/submission/dto';

/**
 * REST API DTO for individual available rewards
 */
export class AvailableRewardRestDto extends AvailableRewardDto {
  // REST-specific customizations can be added here if needed
}

/**
 * REST API DTO for available rewards response
 */
export class AvailableRewardsRestResponseDto extends AvailableRewardsResponseDto {
  // REST-specific customizations can be added here if needed
}