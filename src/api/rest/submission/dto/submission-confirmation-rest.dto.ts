import { SubmissionConfirmationDto } from '../../../../modules/submission/dto';

/**
 * REST API DTO for submission confirmation responses
 * Inherits structure from base SubmissionConfirmationDto
 */
export class SubmissionConfirmationRestDto extends SubmissionConfirmationDto {
  // REST-specific customizations can be added here if needed
}